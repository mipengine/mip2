#!/usr/bin/env node
require('../lib/utils/compat')
const cli = require('../lib/cli')
const init = require('../lib/init')

cli.setup({
  noArgs: true,
  help: [
    '',
    '  Examples:',
    cli.chalk.gray('    # 初始化 MIP 项目'),
    '    $ mip2 init'
  ].join('\n')
})

init()
